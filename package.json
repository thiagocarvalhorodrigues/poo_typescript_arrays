{
  "name": "poo_typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project ./",
    "start": "ts-node-dev --transpile-only --ignore-watch node_modules --respawn src/arrays/foreach.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "nodemon": "^2.0.15",
    "ts-node-dev": "^1.1.8"
  }
}
